/* Autogenerated by mlir-tblgen; don't manually edit */

#ifdef GEN_PASS_DECL
// Generate declarations for all passes.
#define GEN_PASS_DECL_THLOLEGALIZESORTPASS
#undef GEN_PASS_DECL
#endif // GEN_PASS_DECL

//===----------------------------------------------------------------------===//
// ThloLegalizeSortPass
//===----------------------------------------------------------------------===//
#ifdef GEN_PASS_DECL_THLOLEGALIZESORTPASS
#undef GEN_PASS_DECL_THLOLEGALIZESORTPASS
#endif // GEN_PASS_DECL_THLOLEGALIZESORTPASS
#ifdef GEN_PASS_DEF_THLOLEGALIZESORTPASS
namespace impl {

template <typename DerivedT>
class ThloLegalizeSortPassBase : public ::mlir::OperationPass<func::FuncOp> {
public:
  using Base = ThloLegalizeSortPassBase;

  ThloLegalizeSortPassBase() : ::mlir::OperationPass<func::FuncOp>(::mlir::TypeID::get<DerivedT>()) {}
  ThloLegalizeSortPassBase(const ThloLegalizeSortPassBase &other) : ::mlir::OperationPass<func::FuncOp>(other) {}

  /// Returns the command-line argument attached to this pass.
  static constexpr ::llvm::StringLiteral getArgumentName() {
    return ::llvm::StringLiteral("thlo-legalize-sort");
  }
  ::llvm::StringRef getArgument() const override { return "thlo-legalize-sort"; }

  ::llvm::StringRef getDescription() const override { return "Legalize from THLO sort with buffer semantics to SCF control flow."; }

  /// Returns the derived pass name.
  static constexpr ::llvm::StringLiteral getPassName() {
    return ::llvm::StringLiteral("ThloLegalizeSortPass");
  }
  ::llvm::StringRef getName() const override { return "ThloLegalizeSortPass"; }

  /// Support isa/dyn_cast functionality for the derived pass class.
  static bool classof(const ::mlir::Pass *pass) {
    return pass->getTypeID() == ::mlir::TypeID::get<DerivedT>();
  }

  /// A clone method to create a copy of this pass.
  std::unique_ptr<::mlir::Pass> clonePass() const override {
    return std::make_unique<DerivedT>(*static_cast<const DerivedT *>(this));
  }

  /// Return the dialect that must be loaded in the context before this pass.
  void getDependentDialects(::mlir::DialectRegistry &registry) const override {
    
  registry.insert<arith::ArithDialect>();

  registry.insert<memref::MemRefDialect>();

  registry.insert<scf::SCFDialect>();

  }

  /// Explicitly declare the TypeID for this class. We declare an explicit private
  /// instantiation because Pass classes should only be visible by the current
  /// library.
  MLIR_DEFINE_EXPLICIT_INTERNAL_INLINE_TYPE_ID(ThloLegalizeSortPassBase<DerivedT>)

protected:
private:
};
} // namespace impl
#undef GEN_PASS_DEF_THLOLEGALIZESORTPASS
#endif // GEN_PASS_DEF_THLOLEGALIZESORTPASS
#ifdef GEN_PASS_REGISTRATION

//===----------------------------------------------------------------------===//
// ThloLegalizeSortPass Registration
//===----------------------------------------------------------------------===//

inline void registerThloLegalizeSortPass() {
  ::mlir::registerPass([]() -> std::unique_ptr<::mlir::Pass> {
    return createLegalizeSortPass();
  });
}

// Old registration code, kept for temporary backwards compatibility.
inline void registerThloLegalizeSortPassPass() {
  ::mlir::registerPass([]() -> std::unique_ptr<::mlir::Pass> {
    return createLegalizeSortPass();
  });
}

//===----------------------------------------------------------------------===//
// AllThlo Registration
//===----------------------------------------------------------------------===//

inline void registerAllThloPasses() {
  registerThloLegalizeSortPass();
}
#undef GEN_PASS_REGISTRATION
#endif // GEN_PASS_REGISTRATION
// Deprecated. Please use the new per-pass macros.
#ifdef GEN_PASS_CLASSES

template <typename DerivedT>
class ThloLegalizeSortPassBase : public ::mlir::OperationPass<func::FuncOp> {
public:
  using Base = ThloLegalizeSortPassBase;

  ThloLegalizeSortPassBase() : ::mlir::OperationPass<func::FuncOp>(::mlir::TypeID::get<DerivedT>()) {}
  ThloLegalizeSortPassBase(const ThloLegalizeSortPassBase &other) : ::mlir::OperationPass<func::FuncOp>(other) {}

  /// Returns the command-line argument attached to this pass.
  static constexpr ::llvm::StringLiteral getArgumentName() {
    return ::llvm::StringLiteral("thlo-legalize-sort");
  }
  ::llvm::StringRef getArgument() const override { return "thlo-legalize-sort"; }

  ::llvm::StringRef getDescription() const override { return "Legalize from THLO sort with buffer semantics to SCF control flow."; }

  /// Returns the derived pass name.
  static constexpr ::llvm::StringLiteral getPassName() {
    return ::llvm::StringLiteral("ThloLegalizeSortPass");
  }
  ::llvm::StringRef getName() const override { return "ThloLegalizeSortPass"; }

  /// Support isa/dyn_cast functionality for the derived pass class.
  static bool classof(const ::mlir::Pass *pass) {
    return pass->getTypeID() == ::mlir::TypeID::get<DerivedT>();
  }

  /// A clone method to create a copy of this pass.
  std::unique_ptr<::mlir::Pass> clonePass() const override {
    return std::make_unique<DerivedT>(*static_cast<const DerivedT *>(this));
  }

  /// Return the dialect that must be loaded in the context before this pass.
  void getDependentDialects(::mlir::DialectRegistry &registry) const override {
    
  registry.insert<arith::ArithDialect>();

  registry.insert<memref::MemRefDialect>();

  registry.insert<scf::SCFDialect>();

  }

  /// Explicitly declare the TypeID for this class. We declare an explicit private
  /// instantiation because Pass classes should only be visible by the current
  /// library.
  MLIR_DEFINE_EXPLICIT_INTERNAL_INLINE_TYPE_ID(ThloLegalizeSortPassBase<DerivedT>)

protected:
};
#undef GEN_PASS_CLASSES
#endif // GEN_PASS_CLASSES
